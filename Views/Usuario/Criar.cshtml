@{
    ViewData["Title"] = "Criar Usuário";
}

<div class="panel-card">
    <div class="panel-header">
        <h1 class="panel-title"><span style="opacity:0.7; margin-right:10px;">👤</span> Novo Usuário</h1>
        <p class="panel-subtitle">Preencha os dados para registrar no AD</p>
    </div>

    <div class="panel-body">
        @if (ViewBag.MensagemErro != null)
        {
            <div class="alert alert-danger small py-2 mb-4 border-0 rounded-1">@ViewBag.MensagemErro</div>
        }
        @if (ViewBag.MensagemSucesso != null)
        {
            <div class="alert alert-success small py-2 mb-4 border-0 rounded-1">@ViewBag.MensagemSucesso</div>
        }

        <form asp-action="Criar" method="post">
            <div class="mb-3">
                <label class="form-label">Login</label>
                <input type="text" name="Login" class="form-control" required placeholder="ex: joao.paçoca" />
            </div>
            
            <div class="mb-3">
                <label class="form-label">Senha Inicial</label>
                <input type="password" name="Senha" class="form-control" required />
            </div>

            <div class="mb-4">
                <label class="form-label">Grupo (Opcional)</label>
                <input type="text" name="Grupo" class="form-control" placeholder="ex: Analistas" />
            </div>

            <div class="d-grid gap-2">
                <button type="submit" class="btn btn-primary">CRIAR ACESSO</button>
                <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary border-0" style="color: #aaa;">Voltar ao Menu</a>
            </div>
        </form>
    </div>
</div>